name: Docker Build

run-name: Docker Build:${{ github.ref_name }}(${{ github.event.head_commit.message }})

on:
  push:

jobs:
  docker-build:
    runs-on: ubuntu-22.04

    permissions:
      packages: write
      contents: read

    steps:
      - name: Build and push
        uses: miyajimad0620/traps-github-action/docker-build-ros2-pkg@tmp
        with:
          # token: ${{ secrets.PAT }}
          platforms: llinux/arm64/v8
          # rosdistro: humble
